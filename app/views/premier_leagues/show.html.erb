<% provide :title, @premier_league.name
   provide :breadcrumbs, breadcrumbs([ :class , :select_unique_name ]) %> 
  
<h1><%= @premier_league.name %> <%= @premier_league.season.name %></h1>
<%= render 'layouts/administration_links', resource: @premier_league %>
