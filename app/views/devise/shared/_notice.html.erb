<% if !notice.nil? && !notice.empty? %>
  <div class="alert alert-success"><%= notice %></div>
<% end %>
<% if !flash.nil? && !flash[:devise_alert].blank? %>
  <div class="alert alert-danger"><%= flash[:devise_alert] %></div>
<% end %>
