<% provide :title, "Season #{@season.name}"
   provide :breadcrumbs, render('layouts/breadcrumbs', breadcrumbs: [ link_to("Seasons", seasons_path) ], select_for: Season ) %> 


<h1>Season <%= @season.name %></h1>
<%= render 'shared/administration_links', resource: @season %>
